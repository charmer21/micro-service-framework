<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.companyname.webapp.domain.DemoRepository">

    <select id="findAll" resultType="com.companyname.webapp.domain.Demo">
        select * from demo;
    </select>

    <insert id="save">
        insert into demo(id, name, mobile) values(#{id}, #{name}, #{mobile})
    </insert>
</mapper>