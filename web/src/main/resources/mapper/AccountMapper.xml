<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.companyname.framework.security.AccountMapper">

    <resultMap id="account" type="com.companyname.framework.security.Account">
        <result property="loginId" column="login_id"/>
        <result property="userName" column="user_name"/>
    </resultMap>
    
    <select id="findByLoginId" resultMap="account">
        select u.* from users u where u.login_id = #{loginid};
    </select>

    <select id="getRoles" resultType="com.companyname.framework.security.Role">
        select r.* from users u
        inner join user_roles ur on u.login_id = ur.login_id
        inner join roles r on ur.role_id = r.id
        where u.login_id = #{loginid};
    </select>

    <select id="hasPermission" resultType="Boolean" useCache="true">
        select count(1) from permissions p
        inner join role_permissions rp on p.id = rp.permission_id
        inner join user_roles ur on rp.role_id = ur.role_id
        where ur.login_id = #{0} and p.code = #{1};
    </select>

</mapper>